# apps

`web/` — the Next.js application. **The canonical product.** Scaffolded in Phase 0,
built out from Phase 1 onward.

`desktop/` — the Tauri macOS shell. Post-MVP (Phase 15). Wraps the shared frontend;
never a separate implementation.
