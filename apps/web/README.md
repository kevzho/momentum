# @momentum/web

The Next.js application — the canonical Momentum product. See `docs/ARCHITECTURE.md`
for the directory layout, server/client boundaries, and data-access rules.

```
pnpm dev         # from the repo root
pnpm typecheck   # runs `next typegen` then `tsc --noEmit`
```
