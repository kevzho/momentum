# @momentum/db

Data-access layer: generated database types, row↔domain mappers, and repository
functions parameterized by a typed Supabase client. Implemented in Phase 2 against the
schema in `docs/DATABASE.md`. Framework-free (lint-enforced).
